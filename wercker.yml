box: node
build:
  steps:
    - script:
        name: mobrix-designer lib build
        code: |
          npm i
          npm run build
test:
  steps:
    - script:
        name: mobrix-designer lib tests
        code: |
          npm i
          npm run test
